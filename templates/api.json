{
    "error": "access_denied",
    "message": "Your IP address has been blocked",
    "client_ip": "{{client_ip}}",
    "request_method": "{{request_method}}",
    "request_uri": "{{request_uri}}",
    "reason": "{{reason}}"
}
