# Hub AppSec tests

See [testing.md](testing.md#hub-appsec--full-rule-catalog-local) for setup, coverage numbers, and commands.

Quick reference:

```bash
./scripts/hubtest-target.sh up
./scripts/hubtest-run.sh --all
./scripts/hubtest-coverage.sh --percent
```
